importar dht
importar máquina
importar red
importar tiempo

Nombre de la red y contraseña para el Punto de Acceso
SSID = "NOMBRE DE LA RED"
PASSWORD = "CLAVE"

Configuración del punto de acceso
ap = network.WLAN(network.AP_IF)
ap.active(True)
ap.config(essid=SSID, contraseña=CONTRASEÑA)

ip_address = ap.ifconfig()[0]
print("ESP32 configurado como Access Point")
print("Dirección IP del Access Point:", ip_address)

Señal del sensor en el pin 4
dht_sensor = dht.DHT11(machine.Pin(23))